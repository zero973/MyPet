<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.student3.myfavouritepet">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".GameSchool" android:label="Школа"></activity>
        <activity android:name=".Room" android:label="Комната"></activity>
        <activity android:name=".MathActivity" android:label="Математика"></activity>
        <activity android:name=".PhisActivity" android:label="Физика"></activity>
        <activity android:name=".RusActivity" android:label="Русский язык"></activity>
        <activity android:name=".EnglishActivity" android:label="Английский язык"></activity>
        <activity android:name=".RusActivity_anagrams" android:label="Русский язык"></activity>
        <activity android:name=".MathActivityDescrim" android:label="Математика"></activity>
        <activity android:name=".PhisActivityTasks" android:label="Физика"></activity>
    </application>

</manifest>